import { type Container, type IContainerPlugin, type IPlugin } from "@tsparticles/engine";
export declare class OverlapPlugin implements IPlugin {
    id: string;
    getPlugin(container: Container): Promise<IContainerPlugin>;
    loadOptions(): void;
    needsPlugin(): boolean;
}
