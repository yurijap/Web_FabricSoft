import { AnimationOptions } from "./AnimationOptions.js";
import type { IColorAnimation } from "../Interfaces/IColorAnimation.js";
import type { RangeValue } from "../../Types/RangeValue.js";
import type { RecursivePartial } from "../../Types/RecursivePartial.js";
export declare class ColorAnimation extends AnimationOptions implements IColorAnimation {
    max: number;
    min: number;
    offset: RangeValue;
    sync: boolean;
    constructor(min: number, max: number);
    protected doLoad(data: RecursivePartial<IColorAnimation>): void;
}
